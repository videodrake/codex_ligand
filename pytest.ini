[pytest]
markers =
    unit: fast deterministic tests for isolated functions/modules
    integration: cross-module tests with local filesystem wiring
    e2e: end-to-end scenario tests
    slow: long-running tests excluded from default PR checks
    reporting: user-facing report, review, or presentation artifact tests that are useful for full regression but non-blocking for pre-qsub validation
