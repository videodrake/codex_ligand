# EGFR-MYO1D 파이프라인 투두리스트

> 코드 미구현, 서버 실행, 분석 스크립트, 사람 결정, 코드 정리 — 전체 미완료 항목 통합 문서.
> 2026-03-24 기준. 완료되면 각 항목에 날짜를 기록하세요.

---

## 0. 코드 내 미구현 (Placeholder / TODO)

아직 구현되지 않았거나 주석 처리된 코드 경로입니다.

### 0.1 Plan E-5 면책 조항 (valid_sites_disclaimer.md)

**현재:** `run_production.py:1003-1007`에서 Phase 5 상태 판정 시 `valid_sites_disclaimer.md`를 체크하던 코드가 주석 처리됨 (optional).
**필요:** Plan E-5 구현 후 면책 조항 생성 로직을 `verdict.py`에 추가하고, `run_production.py`에서 필수 체크로 전환.

- [ ] Plan E-5 면책 조항 생성 로직 구현
- [ ] `run_production.py` 주석 해제하여 필수 전환

**완료:** ____년 __월 __일

---

### 0.2 FTMap 파싱 미구현

**현재:** CLI에서 `--ftmap-dir` 인자를 받지만, `druggability_confidence.py:345-352`의 파싱 코드가 주석 처리됨. 출력 CSV에 `ftmap_hotspot_count`, `ftmap_overlap_residues`, `hotspot_support_available` 컬럼이 빈 값으로 존재.
**영향:** druggability confidence에 fragment hotspot 데이터가 반영되지 않음.

- [ ] FTMap 서버 실행 또는 로컬 설치
- [ ] `druggability_confidence.py`에서 FTMap 파싱 코드 주석 해제 및 검증

**완료:** ____년 __월 __일

---

### 0.3 FreeSASA 표면적 분석 (B-1.1)

**현재:** `region_definitions.py:10-13`의 309개 잔기 surface/core 분류가 문헌 근사치.
**필요:** 실제 FreeSASA 계산(SASA > 10.0 Å²) 후 영역 분류 업데이트.

**방법:**
```bash
freesasa --format=seq input/receptors/3GT8_raw.pdb > sasa_output.txt
# 결과를 파싱하여 region_definitions.py 업데이트
```

- [ ] FreeSASA 실행 및 영역 분류 업데이트

**완료:** ____년 __월 __일

---

### 0.4 Workflow A↔B 비교 — 실데이터 모드

**현재:** `workflow_comparison.py`에서 `--synthetic` 모드만 동작. 실데이터 경로 로딩이 미완성.
**필요:** 서버 실행(1.1/1.2) 완료 후 실데이터 경로 로딩 구현 추가.

- [ ] 실데이터 경로 로딩 구현 (1.1/1.2 완료 후)

**완료:** ____년 __월 __일

---

## 1. 서버에서 실행해야 하는 것들

이 항목들은 HPC 서버(node05)에서 qsub로 실행해야 합니다.
코드는 모두 준비되어 있고, 실행만 하면 됩니다.

### 1.1 프로덕션 파이프라인 재실행

**왜:** 코드 수정 사항(cross_coverage 차등, handoff 가드, allosteric 태깅 등)이 반영된 결과를 생성하기 위해.

**방법:**
```bash
# 전체 재실행 (Phase 1부터)
qsub config/run_production.pbs

# 또는 Verdict부터만 재실행 (Phase 1-4 결과가 이미 있다면)
python run_production.py --from 5
```

**확인 사항:**
- [ ] 모든 Phase가 `[OK]`로 완료되는지
- [ ] "Derived Step View: 7/7 generated"가 표시되는지
- [ ] exit code가 0인지 (echo $?)
- [ ] `output/workflow_a/phase5_verdict/valid_sites.csv` 생성 확인
- [ ] `output/workflow_a/phase6_report/project_report.txt`가 빈 파일이 아닌지 (최소 수 KB)

**완료:** ____년 __월 __일

---

### 1.2 Workflow B (Advanced Pipeline) 실행

**왜:** Phase 4 perturbation scoring 결과를 생성하여 Workflow A와 비교하기 위해.

**방법:**
```bash
# precheck 포함 자동 실행
qsub config/run_advanced_pipeline.pbs

# 또는 특정 Phase부터
qsub -v ADV_FROM=2 config/run_advanced_pipeline.pbs
```

**사전 조건:**
- Workflow A Phase 2 (PPI docking) 완료 필수
- fpocket 설치 확인: `which fpocket`

**확인 사항:**
- [ ] Phase 1~4 모두 완료
- [ ] `output/workflow_b/phase4_scoring/phase4_final_review_table.csv` 존재
- [ ] precheck에서 fpocket/P2Rank/LightDock 가용성 확인 통과

**완료:** ____년 __월 __일

---

### 1.3 Fragment 범위 Pilot 실행 (AC-3.3)

**왜:** MYO1D fragment 범위(945-1006 / 955-1006 / 955-1015)에 따라 PPI hotspot이 어떻게 변하는지 확인. 현재 범위(955-1006)가 적절한지 검증.

**방법:**
```bash
# 3개 범위 각각 제출 (각각 ~6시간, 2000 모델)
qsub -v RANGE=A config/run_pilot_fragment.pbs   # 945-1006 (N-terminal +10)
qsub -v RANGE=B config/run_pilot_fragment.pbs   # 955-1006 (현재)
qsub -v RANGE=C config/run_pilot_fragment.pbs   # 955-1015 (C-terminal +9)

# 완료 후 비교
python scripts/pilot_fragment_range.py --compare
```

**판단 기준:**
- Jaccard ≥ 0.7: 범위에 robust → 현재 유지
- Jaccard 0.5~0.7: 부분적 영향 → 검토 필요
- Jaccard < 0.3 전체: **결정적 영향** → 구조적 검토 후 범위 재선택

**확인 사항:**
- [ ] 3개 pilot 완료
- [ ] `output/pilot_fragment_comparison.csv` 생성
- [ ] Jaccard 값 확인 후 범위 확정 결정 기록

**완료:** ____년 __월 __일 / **결정:** _________________________

---

### 1.4 리간드 다양성 평가 스크립트 실행 (AC-1.1)

**왜:** 3종 리간드의 Tanimoto 유사도가 0.4 미만인지 정량적으로 확인. 현재 테스트 2건이 이 파일 미존재로 실패 중.

**방법:**
```bash
# RDKit 환경 필요
conda activate rdkit_env  # 또는 pyrosetta에 rdkit 설치
python scripts/assess_ligand_diversity.py
```

**사전 조건:**
- `input/ligands/` 디렉토리에 SDF 3종 존재
- RDKit 설치 필요 (`from rdkit import Chem`)

**확인 사항:**
- [ ] `output/ligand_diversity_assessment.csv` 생성
- [ ] 모든 쌍별 Tanimoto < 0.4 확인
- [ ] 이후 pytest 재실행 시 2건 실패 해소

**완료:** ____년 __월 __일

---

## 2. 실제 데이터로 재실행해야 하는 분석 스크립트

서버 실행(1.1/1.2) 완료 후 로컬에서 실행 가능합니다.

### 2.1 가중치 민감도 시뮬레이션 (AC-4.1)

**왜:** 합성 데이터가 아닌 실제 포켓 점수로 6개 가중치 조합의 영향을 확인.

**방법:**
```bash
python scripts/verdict_weight_sensitivity.py \
  --input output/workflow_a/phase5_verdict/valid_sites.csv \
  --output_dir output/
```

**확인 사항:**
- [ ] `verdict_weight_sensitivity.csv`에서 판정 변화 포켓 확인
- [ ] PPI 가중치 증가로 STRONG 승격되는 포켓이 있는지
- [ ] "가중치에 robust" 판정이 나오는지, 아니면 민감한 포켓이 있는지

**→ 결정 필요: 현재 50/20/30 유지할 것인지, 변경할 것인지**

**완료:** ____년 __월 __일 / **결정:** _________________________

---

### 2.2 PPI 거리 분포 분석 (AC-4.4)

**왜:** 현재 임계값(8/15/25Å)이 실제 포켓 분포를 적절히 분할하는지 확인. 70% 이상이 한 구간에 몰리면 세분화 필요.

**방법:**
```python
# valid_sites.csv에서 spatial_dist_to_ppi 컬럼 추출 후
from scripts.verdict_weight_sensitivity import analyze_ppi_distance_distribution

distances = [float(r["spatial_dist_to_ppi"]) for r in rows if r["spatial_dist_to_ppi"]]

# 오프셋 없이
rows_raw, warnings = analyze_ppi_distance_distribution(distances)
# 4Å 오프셋 적용
rows_off, warnings = analyze_ppi_distance_distribution(distances, offset=4.0)
```

**확인 사항:**
- [ ] 구간별 포켓 비율 확인 (adjacent/near/moderate/distant)
- [ ] 70% 집중 구간이 있는지
- [ ] 오프셋(3-5Å) 적용 시 분류가 어떻게 변하는지

**→ 결정 필요: 임계값 조정이 필요한지 (현재 8/15/25Å)**

**완료:** ____년 __월 __일 / **결정:** _________________________

---

### 2.3 Centroid 오프셋 보정 시뮬레이션 (AC-4.2)

**왜:** pocket_depth에 따른 centroid 보정이 PPI proximity 판정을 몇 개나 바꾸는지 확인.

**방법:**
```bash
python scripts/centroid_offset_analysis.py \
  --input output/workflow_a/phase5_verdict/valid_sites.csv \
  --output_dir output/
```

**확인 사항:**
- [ ] `centroid_offset_correction.csv`에서 verdict_change=True인 포켓 수
- [ ] 5개 이상이면 **경고**: 개별 포켓의 생물학적 의미 확인 필요 (EC-4.2)
- [ ] alpha 값별 (0.5~1.0) 변화 추이 확인

**→ 결정 필요: 보정 적용할 것인지, 적용 시 alpha 값 선택**

**완료:** ____년 __월 __일 / **결정:** _________________________

---

### 2.4 Workflow A↔B 비교 실행 (AC-5.1)

**왜:** 두 워크플로우의 포켓을 매칭하여 Consensus/A-only/B-only/Conflict 분류.

**방법:**
```bash
# Workflow A + B 모두 완료 후
python -m egfr_pipeline.workflow_comparison \
  --a-verdict output/workflow_a/phase5_verdict/valid_sites.csv \
  --a-pockets output/workflow_a/phase4_vina_postprocess/vina_pocket_table.csv \
  --b-review output/workflow_b/phase4_scoring/phase4_final_review_table.csv \
  --b-pockets output/phase2_pockets/candidate_pockets.csv \
  --output_dir output/
```
(⚠ 실데이터 모드 구현은 0.4 항목 참조)

**확인 사항:**
- [ ] `workflow_comparison.csv` 생성
- [ ] Consensus 포켓 목록 → 실험 최우선 후보
- [ ] Conflict 포켓 → PyMOL에서 수동 확인

**완료:** ____년 __월 __일

---

## 3. 사람이 판단해야 하는 결정 사항

코드는 준비되어 있지만, 최종 값/방향은 사람이 결정해야 합니다.

### 3.1 Cross-receptor 차등 점수 확정

**현재:** 3/3=20, 2/3=14, 1/3=0 (verdict.py DEFAULT_THRESHOLDS)
**PRD 원문:** 1/3→10-15, 2/3→22-24, 3/3→30
**변경 근거:** 1/3 상태 포켓은 support quality로만 점수 획득, 단일 상태 존재만으로는 coverage 미부여

**실제 데이터 확인 후 결정:**
- [ ] cross_coverage_2of3 = 14 유지? 또는 PRD대로 22로 변경?
- [ ] cross_coverage_1of3 = 0 유지? 또는 PRD대로 10으로 복원?

설정 변경 방법: `config.yaml`의 `verdict:` 섹션에서 override 가능
```yaml
verdict:
  cross_coverage_3of3: 20
  cross_coverage_2of3: 14  # 또는 22
  cross_coverage_1of3: 0   # 또는 10
```

---

### 3.2 Allosteric 후보 임계값

**현재:** Vina ≥ 35 AND PPI ≤ 5
**근거:** Vina 만점 50의 70%, PPI 만점 20의 25%

**실제 데이터 확인 후 결정:**
- [ ] allosteric 후보가 너무 많으면 → Vina 임계값 상향 (예: 40)
- [ ] allosteric 후보가 0이면 → 정상 (EC-5.2), 임계값 하향 검토

---

### 3.3 가중치 배분 최종 확정

**현재:** Vina 50 / PPI 20 / Cross 30 (합성 데이터 시뮬레이션에서 robust)
**대안:** PPI 강화(40/30/30), 균등(33/33/34) 등 6개 조합 준비됨

**실제 데이터 시뮬레이션(2.1) 완료 후 결정:**
- [ ] STRONG 포켓 집합이 가중치에 robust한지 확인
- [ ] PPI 승격 포켓이 있다면 생물학적 의미 검토
- [ ] 최종 배분 확정 후 `config.yaml` 또는 DEFAULT_THRESHOLDS 반영

---

### 3.4 PPI 거리 임계값 조정 여부

**현재:** adjacent < 8Å, near < 15Å, moderate < 25Å
**Centroid 오프셋:** Vina centroid가 포켓 내부에 있어 실제보다 3-5Å 과대 측정

**실제 거리 분포(2.2) 확인 후 결정:**
- [ ] 70% 이상 한 구간 집중 시 → 세분화 제안 검토
- [ ] 오프셋 보정 적용 여부 결정 (2.3 결과 참조)

---

## 4. 코드 정리 (영향 범위 판단 필요)

### 4.1 verdict.py _MEMBRANE_RESIDUES 잔기 747 누락

**현재:** `_MEMBRANE_RESIDUES`에 747이 없으나, `generate_configs.py`와 `prepare_inputs.py`에는 포함됨.
**영향:** membrane_face overlap 판정이 불완전할 수 있음.
**수정:** verdict.py의 `_MEMBRANE_RESIDUES` set에 747 추가 (1줄).

- [ ] 747 추가 수정 적용 여부

---

### 4.2 NLOBE_CLOBE_BOUNDARY 7파일 중복

**현재:** 값 838이 7개 파일에 각각 하드코딩됨.
**위험:** 값 변경 시 7곳 모두 수정 필요, 누락 가능성.
**수정:** `region_definitions.py`에 상수 정의 후 각 파일에서 import.

- [ ] 중앙화 수정 적용 여부

---

### 4.3 SHEET_DEFINITIONS 2곳 중복

**현재:** `prepare_inputs.py`와 `orientation_filter.py`에 동일 정의.
**수정:** `region_definitions.py`에서 중앙 관리.

- [ ] 중앙화 수정 적용 여부

---

## 5. 실행 순서 권장

```
1.4 (리간드 다양성)     ← RDKit만 있으면 바로 실행 가능
    ↓
1.1 (프로덕션 재실행)   ← 가장 중요, 수 시간 소요
    ↓
2.1 (가중치 시뮬레이션) ← valid_sites.csv 필요
2.2 (거리 분포)         ← valid_sites.csv 필요
2.3 (오프셋 보정)       ← valid_sites.csv + pocket_depth 필요
    ↓
3.1~3.4 (결정)          ← 위 분석 결과 기반
    ↓
1.2 (Workflow B)        ← Workflow A 완료 후
    ↓
2.4 (A↔B 비교)         ← 양쪽 완료 후
    ↓
1.3 (Fragment pilot)    ← 독립 실행 가능, 시간 소요
    ↓
0.1~0.4 (코드 미구현)   ← 우선순위/필요에 따라 수시 진행
4.1~4.3 (코드 정리)     ← 우선순위/필요에 따라 수시 진행
```
